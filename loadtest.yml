config:
  target: 'https://localhost:7102'
  phases:
    - duration: 60
      arrivalRate: 10
scenarios:
  - flow:
    - patch:
        url: "/acumulador?acumulo=100"
        json:
          acumulo: 1
